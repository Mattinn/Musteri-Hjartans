<%= form_for(@personal_test) do |f| %>
  <% if @personal_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@personal_test.errors.count, "error") %> prohibited this personal_test from being saved:</h2>

      <ul>
      <% @personal_test.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Nafn" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Lýsing" %><br />
    <%= f.text_area :description %>
  </div>
  
  <div class="field">
  	<%= f.fields_for :questions do |builder| %>
    <%= render "question_fields", :f => builder %>
  <% end %>
  </div>
  
  <p><%= link_to_add_fields "Add Question", f, :questions %></p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
