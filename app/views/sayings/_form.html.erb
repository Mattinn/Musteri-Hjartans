<div class="form">
 <%= simple_form_for(@saying) do |f| %>

   <div class="form-inputs">
     <%= f.input :message, :label => false %>
   </div>

   <div class="form-actions">
     <%= f.button :submit, 'Vista' %>
   </div>
 <% end %>
</div>
